/*import React, { ReactNode } from "react";

import Telemetry from "./react";
export default function (children: ReactNode) {
  return (
    <Telemetry.Provider project_name="POC">
      <Telemetry.Publishers.Prometheus.Counter prometheus_url="" help="" />
      {children}
    </Telemetry.Provider>
  );
}
*/
